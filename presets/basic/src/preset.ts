import { tsParticles } from "tsparticles-core";

tsParticles.addPreset("basic", {
    particles: {
        lineLinked: {
            enable: true,
        },
        move: {
            enable: true,
        },
    },
});
